# 🎨 Data Center Monitoring App - Visual Architecture Guide

## 🐳 Docker Container Architecture

```
┌─────────────────────────────────────────────────────────────────────────┐
│                                                                          │
│                    Docker Container: dual-terminal-app                   │
│                              Port 80 (HTTP)                              │
│                                                                          │
│  ┌─────────────────────────────────────────────────────────────────┐   │
│  │                         NGINX (Port 80)                          │   │
│  │  ┌───────────┐  ┌──────────────┐  ┌───────────────────────┐   │   │
│  │  │  Static   │  │   /api/*     │  │  /socket.io/*        │   │   │
│  │  │  Files    │  │   Proxy      │  │  WebSocket Proxy     │   │   │
│  │  │  (React)  │  │              │  │                      │   │   │
│  │  └─────┬─────┘  └──────┬───────┘  └──────────┬───────────┘   │   │
│  └────────┼────────────────┼────────────────────┼───────────────┘   │
│           │                │                     │                     │
│           │                └─────────────────────┘                     │
│           │                          │                                 │
│           │                          ▼                                 │
│  ┌────────▼──────────────────────────────────────────────────────┐   │
│  │             FastAPI + Socket.IO Backend (Port 8000)            │   │
│  │  ┌──────────────┐  ┌──────────────┐  ┌──────────────────┐   │   │
│  │  │     Auth     │  │   Terminal   │  │   Socket.IO      │   │   │
│  │  │  (JWT/OAuth) │  │   Manager    │  │   Real-time      │   │   │
│  │  │              │  │   (PTY)      │  │   Events         │   │   │
│  │  └──────┬───────┘  └──────┬───────┘  └─────────┬────────┘   │   │
│  └─────────┼──────────────────┼───────────────────┼────────────┘   │
│            │                  │                    │                   │
│            └──────────────────┴────────────────────┘                   │
│                               │                                        │
│                               ▼                                        │
│  ┌────────────────────────────────────────────────────────────────┐   │
│  │                    SQLAlchemy ORM                               │   │
│  │  ┌───────────────────────────────────────────────────────┐    │   │
│  │  │       SQLite Database (Persistent Volume)             │    │   │
│  │  │       /app/data/db/app.db                             │    │   │
│  │  │  ┌──────────┐  ┌──────────────┐  ┌───────────────┐  │    │   │
│  │  │  │  Users   │  │  Sessions    │  │  Terminals    │  │    │   │
│  │  │  └──────────┘  └──────────────┘  └───────────────┘  │    │   │
│  │  └───────────────────────────────────────────────────────┘    │   │
│  └────────────────────────────────────────────────────────────────┘   │
│                                                                         │
│  ┌────────────────────────────────────────────────────────────────┐   │
│  │              Supervisord (Process Manager)                      │   │
│  │  ┌─────────────────────┐  ┌─────────────────────────────┐     │   │
│  │  │   Backend Process   │  │     Nginx Process           │     │   │
│  │  │   Priority: 10      │  │     Priority: 999           │     │   │
│  │  │   Auto-restart: Yes │  │     Auto-restart: Yes       │     │   │
│  │  └─────────────────────┘  └─────────────────────────────┘     │   │
│  └────────────────────────────────────────────────────────────────┘   │
│                                                                         │
│  Volumes:                                                              │
│  📁 /app/data → Docker Volume (Persistent)                            │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

---

## 🔄 Request Flow

### HTTP Request (Frontend Static Files)

```
User Browser
    │
    │ GET http://localhost/
    ▼
┌────────────────────┐
│  NGINX (Port 80)   │
│  Location: /       │
│  Root: /usr/share/ │
│       nginx/html   │
└────────┬───────────┘
         │
         │ Serve static files
         ▼
    ┌─────────┐
    │ React   │
    │ App     │
    │ (SPA)   │
    └─────────┘
```

### API Request

```
React App
    │
    │ POST http://localhost/api/auth/login
    ▼
┌────────────────────┐
│  NGINX (Port 80)   │
│  Location: /api/   │
│  Proxy to:         │
│  localhost:8000    │
└────────┬───────────┘
         │
         │ HTTP Proxy
         ▼
┌─────────────────────┐
│ FastAPI (Port 8000) │
│ Endpoint:           │
│ /api/auth/login     │
└────────┬────────────┘
         │
         │ Validate credentials
         ▼
┌──────────────┐
│  Database    │
│  Query User  │
└──────┬───────┘
       │
       │ User found
       ▼
   Generate JWT
       │
       │ Return token
       ▼
    Browser
```

### WebSocket Connection (Terminal I/O)

```
React App (xterm.js)
    │
    │ Connect ws://localhost/socket.io/
    ▼
┌────────────────────┐
│  NGINX (Port 80)   │
│  Location:         │
│  /socket.io/       │
│  WebSocket Upgrade │
└────────┬───────────┘
         │
         │ WS Proxy
         ▼
┌─────────────────────┐
│ Socket.IO Server    │
│ (Port 8000)         │
│                     │
│ Event: create_term  │
└────────┬────────────┘
         │
         │ Create PTY
         ▼
┌──────────────────────┐
│  Terminal Manager    │
│  - Fork shell        │
│  - Create PTY        │
│  - Track session     │
└────────┬─────────────┘
         │
         │ Stream output
         ▼
   ┌──────────────┐
   │ Shell Process│
   │ /bin/bash    │
   └──────────────┘
         │
         │ Output data
         ▼
┌────────────────────┐
│ Socket.IO          │
│ Emit: term_output  │
└────────┬───────────┘
         │
         ▼
    xterm.js
    (Terminal UI)
```

---

## 🏗️ Multi-Stage Docker Build

```
┌─────────────────────────────────────────────────────────────┐
│                   Stage 1: Frontend Builder                  │
│                   FROM node:18-alpine                        │
├─────────────────────────────────────────────────────────────┤
│  1. Copy package.json, package-lock.json                    │
│  2. RUN npm install                                          │
│  3. Copy frontend source code                                │
│  4. RUN npm run build                                        │
│  5. OUTPUT: /build/dist/ (React static files)               │
└─────────────────────────────────────────────────────────────┘
                             │
                             ▼
┌─────────────────────────────────────────────────────────────┐
│                   Stage 2: Backend Builder                   │
│                   FROM python:3.11-slim                      │
├─────────────────────────────────────────────────────────────┤
│  1. Install build dependencies (gcc, g++, make)             │
│  2. Copy requirements.txt                                    │
│  3. RUN pip install -r requirements.txt                     │
│  4. Copy backend source code                                 │
│  5. OUTPUT: /usr/local/lib/python3.11/site-packages        │
└─────────────────────────────────────────────────────────────┘
                             │
                             ▼
┌─────────────────────────────────────────────────────────────┐
│                   Stage 3: Final Runtime                     │
│                   FROM python:3.11-slim                      │
├─────────────────────────────────────────────────────────────┤
│  1. Install runtime: nginx, supervisor, openssh-client      │
│  2. COPY --from=frontend-builder /build/dist                │
│     → /usr/share/nginx/html/                                 │
│  3. COPY --from=backend-builder Python packages             │
│  4. Copy backend source → /app/backend/                     │
│  5. Configure nginx + supervisord                            │
│  6. Create directories: /app/data, /app/logs                │
│  7. Set environment variables                                │
│  8. EXPOSE 80                                                │
│  9. CMD ["/usr/bin/supervisord", "-c", ...]                 │
└─────────────────────────────────────────────────────────────┘
                             │
                             ▼
                   ┌─────────────────┐
                   │  Docker Image   │
                   │  ~500MB         │
                   └─────────────────┘
```

---

## 📱 Frontend UI Layout (Side-by-Side Terminals)

```
┌────────────────────────────────────────────────────────────────────┐
│  Data Center Monitoring App                                   [User] [Logout]│
├────────────────────────────────────────────────────────────────────┤
│                                                                     │
│  ┌──────────────────────────────┬───────────────────────────────┐ │
│  │  Terminal 1                  │  Terminal 2                   │ │
│  │  ┌────────────────────────┐  │  ┌─────────────────────────┐ │ │
│  │  │ user@host:~$ ls -la    │  │  │ user@host:~$ htop       │ │ │
│  │  │ total 48               │  │  │ ┌─────────────────────┐ │ │ │
│  │  │ drwxr-xr-x  2 user    │  │  │ │ CPU:  ▓▓▓░░░  45%   │ │ │ │
│  │  │ drwxr-xr-x  3 user    │  │  │ │ MEM:  ▓▓▓▓▓░  78%   │ │ │ │
│  │  │ -rw-r--r--  1 user    │  │  │ │ Processes: 142      │ │ │ │
│  │  │                        │  │  │ └─────────────────────┘ │ │ │
│  │  │ user@host:~$ █         │  │  │                         │ │ │
│  │  └────────────────────────┘  │  └─────────────────────────┘ │ │
│  │  [New] [Close] [Resize]     │  [New] [Close] [Resize]       │ │
│  └──────────────────────────────┴───────────────────────────────┘ │
│                                                                     │
│  Status: ● Connected to Socket.IO                                 │
└────────────────────────────────────────────────────────────────────┘
```

---

## 🔐 Authentication Flow

```
┌─────────────────────────────────────────────────────────────────┐
│                        User Registration                         │
└─────────────────────────────────────────────────────────────────┘

User Input                          Backend Processing
┌──────────────┐                    ┌──────────────────────┐
│ Username     │ ──────────────────▶│ 1. Validate input   │
│ Email        │                     │ 2. Check duplicates  │
│ Password     │                     │ 3. Hash password     │
└──────┬───────┘                    │    (bcrypt)          │
       │                             │ 4. Save to DB        │
       │                             └──────┬───────────────┘
       │                                    │
       │                                    ▼
       │                             ┌──────────────────┐
       │                             │ Return User Data │
       │                             │ (no password)    │
       │                             └──────┬───────────┘
       │                                    │
       └────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────┐
│                            User Login                            │
└─────────────────────────────────────────────────────────────────┘

User Input                          Backend Processing
┌──────────────┐                    ┌──────────────────────┐
│ Username     │ ──────────────────▶│ 1. Find user in DB  │
│ Password     │                     │ 2. Verify password   │
└──────┬───────┘                    │    (bcrypt compare)  │
       │                             │ 3. Generate JWT      │
       │                             │    (24h expiry)      │
       │                             └──────┬───────────────┘
       │                                    │
       │                                    ▼
       │                             ┌──────────────────┐
       │                             │ Return JWT Token │
       │                             │ { access_token } │
       │                             └──────┬───────────┘
       │                                    │
       │◀───────────────────────────────────┘
       │
       ▼
┌──────────────┐
│ Store Token  │
│ localStorage │
└──────────────┘

All subsequent requests:
Authorization: Bearer <token>
```

---

## 🔄 Terminal Creation Flow

```
User Action                 Frontend                Backend                  System
┌──────────┐              ┌─────────┐            ┌──────────┐            ┌────────┐
│ Click    │─────────────▶│ Socket  │──────────▶│ Socket.IO│──────────▶│ PTY    │
│ "New     │  emit:       │ Client  │  create_  │ Handler  │  fork()   │ Shell  │
│ Terminal"│  create_term │         │  terminal │          │           │        │
└──────────┘              └────┬────┘            └────┬─────┘            └───┬────┘
                               │                      │                      │
                               │                      │ Create Terminal      │
                               │                      │ Instance             │
                               │                      │                      │
                               │                      │ Assign UUID          │
                               │                      │ terminal_id          │
                               │                      │                      │
                               │◀─────────────────────┤                      │
                               │  terminal_created    │                      │
                               │  {terminal_id}       │                      │
                               │                      │                      │
                               │                      │ Start async read     │
                               │                      │ loop                 │
                               │                      │◀─────────────────────┤
                               │                      │  Shell output        │
                               │◀─────────────────────┤                      │
                               │  terminal_output     │                      │
                               │  {terminal_id, data} │                      │
                               │                      │                      │
                               ▼                      │                      │
                          ┌─────────┐                │                      │
                          │ xterm.js│                │                      │
                          │ Display │                │                      │
                          └─────────┘                │                      │
                                                      │                      │
User Types Command                                    │                      │
┌──────────┐              ┌─────────┐            ┌───▼──────┐            ┌────────┐
│ Type:    │─────────────▶│ Socket  │──────────▶│ Socket.IO│──────────▶│ Shell  │
│ "ls -la" │  emit:       │ Client  │  terminal_│ Handler  │  write()  │ stdin  │
│          │  term_input  │         │  input    │          │           │        │
└──────────┘              └─────────┘            └──────────┘            └────────┘
```

---

## 📦 File Structure in Container

```
/
├── app/
│   ├── backend/
│   │   ├── app/
│   │   │   ├── api/
│   │   │   ├── core/
│   │   │   ├── models/
│   │   │   ├── schemas/
│   │   │   ├── services/
│   │   │   └── main.py
│   │   └── .env
│   ├── data/                    ← Docker Volume (Persistent)
│   │   ├── db/
│   │   │   └── app.db          ← SQLite database
│   │   └── logs/
│   └── logs/
│
├── usr/
│   ├── share/
│   │   └── nginx/
│   │       └── html/            ← React static files
│   │           ├── index.html
│   │           ├── assets/
│   │           │   ├── index-*.js
│   │           │   └── index-*.css
│   │           └── vite.svg
│   │
│   └── local/
│       └── lib/
│           └── python3.11/
│               └── site-packages/  ← Python dependencies
│
├── etc/
│   ├── nginx/
│   │   └── conf.d/
│   │       └── default.conf     ← Nginx config
│   └── supervisord.conf          ← Supervisor config
│
└── var/
    └── log/
        ├── nginx/
        └── supervisor/
```

---

## 🎯 Key Components Interaction

```
┌─────────────────────────────────────────────────────────────────┐
│                     Component Interaction                        │
└─────────────────────────────────────────────────────────────────┘

    React App (Browser)
         │
         │ HTTP/HTTPS
         │
         ▼
    ┌─────────────┐
    │   Nginx     │────── Serves: Static files (/)
    │   :80       │────── Proxies: /api/* → Backend
    └──────┬──────┘────── Proxies: /socket.io/* → Backend
           │
           │ HTTP Proxy
           │
           ▼
    ┌─────────────┐
    │  FastAPI    │
    │  :8000      │
    └──────┬──────┘
           │
           ├──────────────────┬───────────────────┐
           │                  │                    │
           ▼                  ▼                    ▼
    ┌──────────┐      ┌────────────┐      ┌─────────────┐
    │   JWT    │      │  Terminal  │      │  Socket.IO  │
    │   Auth   │      │  Manager   │      │   Events    │
    └────┬─────┘      └─────┬──────┘      └──────┬──────┘
         │                  │                      │
         ▼                  ▼                      │
    ┌──────────────────────────┐                 │
    │   SQLAlchemy (Async)     │                 │
    └────────┬─────────────────┘                 │
             │                                     │
             ▼                                     │
    ┌──────────────┐                             │
    │    SQLite    │                             │
    │   Database   │                             │
    └──────────────┘                             │
                                                  │
                                                  ▼
                                            ┌──────────┐
                                            │   PTY    │
                                            │  Shell   │
                                            └──────────┘
```

---

This visual guide shows how all components work together in the bundled Docker image! 🎨
